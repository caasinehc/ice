ice.colors
	translating colors (hex, rgb, hsl, hsv)
	get/set/inc/dec red, green, blue, hue, sat, lightness, value, shade, tone, tint
	blend
	graph stuff

ice.physics
	collision detection between
		point: line, polygon
		circle: line, polygon
		rectangle: line, polygon
		line: point, circle, rectangle, polygon
		polygon: point, circle, rectangle, line, polygon

ice.dom
  createSlider, createButton, etc.

ice.math
  map(double n, min, max, newMin, newMax)
  new Map(double min, max, newMin, newMax)
  map(double n, Map map)
