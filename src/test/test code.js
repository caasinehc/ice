/*
 * Test code
 * By Isaac Chen
 */

function init() {
	// Init
}
if(typeof ice !== "undefined" && ice.meta.framework.initialized) init();

function tick(dt) {
	// Tick
}

function render() {
	// Render
}