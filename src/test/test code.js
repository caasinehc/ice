/*
 * Test code
 * v1.0.0
 * By Isaac Chen
 * 1/16/2020
 */

function init() {
    // Init
}
if(typeof ice !== "undefined" && ice.meta.framework.initialized) init();

function tick(dt) {
    // Tick
}

function render() {
    // Render
}